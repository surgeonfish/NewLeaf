<Window x:Class="NewLeaf.LeafWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewLeaf"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="Leaf"
        Height="450"
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border x:Name="TittleBar" Grid.Row="0" Background="Transparent">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SaveLeafButton" Content="+"></Button>
                <Button x:Name="MinimizeButton" Content="-"></Button>
                <Button x:Name="MaximizeButton" Content="▢"></Button>
                <Button x:Name="CloseButton" Content="x"></Button>
            </StackPanel>
        </Border>
        <RichTextBox x:Name="TextEditor" Grid.Row="1"></RichTextBox>
    </Grid>
</Window>
