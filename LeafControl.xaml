<UserControl x:Class="NewLeaf.LeafControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NewLeaf"
             mc:Ignorable="d"
             MouseDoubleClick="UserControl_MouseDoubleClick">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MaxWidth="800"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!-- Date Tittle -->
        <TextBlock Grid.Row="0" HorizontalAlignment="Right" Text="{Binding Date}"/>

        <!-- Content -->
        <TextBlock Grid.Row="1" HorizontalAlignment="Left" Text="{Binding Content}"/>
    </Grid>
</UserControl>
